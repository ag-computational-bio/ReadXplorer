############TODO############
||  jritter               ||
|| MA thesis              ||
############################

##TSS Detection##
    - Inputs:
        => Interne TSS excuden
        => zuweit vom Gen liegende TSS (>500bp) excluden
        => leaderless ab wie vielen bp is es eins und wann nicht mehr
    - Outputs (Output von CR mit mehr Inhalt / Daten aus GenBank füllen):
        * WIe genau soll der neue Output aussehen?
        * Soll der alte (default) output die counts an den 10 stellen vor dem TSS 
            behalten?
        * Was möchte man in der Output Tabelle verändern können? Bzw. was will man 
            per Checkboxen checken (ob leaderless oder antisense) ...
            o	Ergebnisse sollten direkt in VAMP darstellbar sein, um z.B. einfacher die TSS zu kontrollieren.
                	hierbei sollte man für eine bessere Übersicht mit Reitern arbeiten oder einschränken können, 
                        welche der Ergebnisse sofort in VAMP angezeigt werden und welche man bei Bedarf dazu holt 
                        oder nur direkt als Excel exportiert haben möchte

    - Algorithmus:
        * Gene to coresponding TSS korrekt zuweisen!
        * Static Regions, brauchen eine solche File dazu

    - Probleme: 


    - Wizard
        - bei Antisense nur ein Bild des einfachsten Falls einfuegen.
        - Bei Leaderless nur ein Bild und die Beschreibung einfuegen.
        - Die Textareas sind aber im Grunde OK.

##OPERON Detection##

##NOVEL REGIONS Detection##

##RPKM/logRPKM##

##Durchgeführte Analysen speicherbar und wieder aufrufbar machen##
    - Das kann man genau so mit einer Action innitialisiere...
        Action -> Load Analyses -> FileChooser -> CSV oder EXCEL Format laden 
            -> Inhalt soll in die TopComponent, falss schon offen, geladen werde,
                sonst eine neue aufmachen... 
    - Man muss nur klären wie diese File aussehen darf.
    - Write ist schon vorhanden in de.cebitec.vamp.exporter.excel using jxl import
        -> Read an existing Excel Spreadsheet (Damit werden wir das lesen von bereits
            durchgeführten Analysen in eine Tabelle) realisieren.

##LEADERLESS Detection
    - o	Potentielle Leaderless-Gene identifizieren und Starts ggfs. korrigieren, 
        z.B. indem man eine Gruppe bildet TSS-TLS <= 7,
        hierzu wären Checkboxen in der Ausgabeliste sinnvoll


##MOTIF SEARCHER##
    - Integration of Improbizer oder BioProspector
        Viktor fragen, wie sie so etwas lösen würden ...


##MA-thesis writing##
    -Latex anpassen

Introduction:
    - RNA Seq
    - ReadXplorer
    - About TSS, RBS, Leaderless, Antisense

Practical Part:




Analysen für Armin:

Ratio: 7
Leaderless 7
interne raus
TSS mit > 500bp raus

=> 1664 hits Cmm_M

=> 1992 hits Cmm_T

Ratio 4

=> Cmm_M 2625 hits

=> Cmm_T 3080 hits

Das Problem ist aber, dass TSS mehrmals vorkommen, da die Mappings nicht alle an einer
Position beginnen. Hier müsste man sich überlegen, ob man dann nicht den TSS nimmt, der die meisten
Counts hat.

Zweites Problem sind überlappende Features zwischen zwei Frames. Problem ist, dass das Frame nicht gesetzt ist.

TODO für die Woche 14.10 - 21.10

- die beiden oberen Probleme lösen!
- Analysen für Armin vorbereiten.
- ist ein CDS-shift vorhanden oder nicht
- Export-Tabelle erweitern
- Operon detection und detection of New Regions voran treiben
- RPKM berechnung voran treiben!

- Die Frage ist, braucht man eig noch die masking Region?